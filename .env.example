APP_NAME=Kickboard
DATABASE_URL=sqlite+pysqlite:///./kickboard.db
# Comma-separated browser origins allowed to call the API, for example:
# CORS_ORIGINS=https://kickboard.example.com,https://www.kickboard.example.com
CORS_ORIGINS=
