/** Brand palette — keep in sync with globals.css `--color-brand` tokens. */
export const BRAND_GRADIENT = {
  start: "#15803d",
  mid: "#16a34a",
  end: "#22c55e"
} as const;

export const BRAND_LIVE = "#ff4757";
